name: Check if pull-request title adheres to conventional commits.
on:
  pull_request:
    types:
      - edited
      - opened
jobs:
  pr-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: .github/actions/commit-lint@main
